# 学習進捗ノート

> このファイルはプロジェクトナレッジ用。区切りが付くたびにClaudeが全文修正版を提示するので、
> 古いファイルを削除して差し替える運用。「変わらないルール」は別途プロジェクトの指示側に置く。

最終更新: 2026-08-22

## 目標

C++を使った3Dゲーム開発職（任天堂等）への就職。学歴不問の応募資格は確認済み。

## 現在地点

**C++ / AtCoder**

- ABS全11問完了 → 現在は実際の過去ABC回に移行中
- A・B問題は安定、C問題は未修得アルゴリズム起因の詰まりが頻発（TLE、フルサーチ止まり）
- 目標：まず緑（800点）
- 習得中：全探索（bit列挙含む）・ソート・累積和・二分探索・map/set
- 次の壁：BFS/DFSなど、緑到達に必要なアルゴリズムの内面化

**環境まわり**

- WSL Ubuntu（Kaliから移行済み）、作業ディレクトリは `~/work/cpp-study/`
- `~/.bash_functions` に `run` `mkabc` `_move_problem` を自力実装済み
- 未完成：`next()` / `prev()` 系（連想配列・正規表現・コマンド置換の理解待ち）

**Unity**

- 本人の意向により一時停止中。再開の意思表示があるまで急かさない。

## 直近のログ（新しい順）

- 2026-08-22: プロジェクトナレッジ／プロジェクトの指示／メモリ機能の役割分担を整理。このノートを新設。

## 次にやること

- [ ] AtCoder過去問でC問題のアルゴリズム引き出しを増やす
- [ ] `next()` 関数の完成
- [ ] （AtCoder緑到達後）Unreal Engineへの移行準備
